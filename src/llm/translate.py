from __future__ import annotations

# NOTE: This module is a placeholder stub.
# Translation is handled by src/pipeline/llm_refiner.py using the Gemini API.
# This file exists for potential future use (e.g. deep-translator for offline fallback).


def translate_message(text: str, target_language: str = "en") -> str:
    """Return the text unchanged (stub — use LLMRefiner for real translation)."""
    return text.strip()
