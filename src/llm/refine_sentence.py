from __future__ import annotations

# NOTE: This module is a placeholder stub.
# Sentence refinement is handled by src/pipeline/llm_refiner.py using the Gemini API.
# This file exists for potential future use (e.g. a lightweight offline fallback).


def refine_sentence(text: str) -> str:
    """Return the text unchanged (stub — use LLMRefiner for real refinement)."""
    return text.strip()
